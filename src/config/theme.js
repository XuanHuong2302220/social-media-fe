import { extendTheme } from "@chakra-ui/react";

const colors = {
  primaryColor: "#4cb5f9",
  iconColor: "#acbac7",
  bgColor: "#06141D",
  itemColor: "#1A2730",
};

const theme = extendTheme({
  colors,
});

export default theme;
